{% extends 'base.html.twig' %}

{% block title %}{% endblock %}

{% block body %}

<div class="example-wrapper">
    <h1>{{ product.name }}</h1>
    <p>{{ product.type }}</p>
    <p>{{ product.description }}</p>
    <p>{{ product.stock }}</p>
</div>
{% endblock %}
